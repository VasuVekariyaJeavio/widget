export default function WidgetNote() {
  return (
    <p className="widget-note" id="contact">
      AvatarForge widget loads in the corner — set <code>data-widget-id</code> and{' '}
      <code>data-avatar-id</code> in the script below.
    </p>
  );
}
